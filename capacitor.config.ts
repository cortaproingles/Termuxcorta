import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cortaproingles.shadowing',
  appName: 'ShadowingApp',
  webDir: 'dist'
};

export default config;
